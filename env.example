# Contact form – Gmail (used by app/api/contact/route.ts)
# For local dev: copy to .env.local in the project root (not in scripts/).
# For Vercel: add these in Project → Settings → Environment Variables (Production + Preview).

# Gmail address that sends the email (optional; defaults to mytajcy@gmail.com)
GMAIL_USER=mytajcy@gmail.com

# Gmail App Password – required. Do NOT use your normal Gmail password.
# Create one at: https://myaccount.google.com/apppasswords (2-Step Verification must be on).
GMAIL_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx
